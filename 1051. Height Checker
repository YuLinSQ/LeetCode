class Solution(object):
    def heightChecker(self, heights):
        """
        :type heights: List[int]
        :rtype: int
        """
        temp = heights[:]
        temp.sort()
        count = 0
        for i in range(len(heights)):
            if heights[i] != temp[i]:
                count+=1
        return count
