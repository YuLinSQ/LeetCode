class Solution {
public:
    bool isValid(string s) {
        stack<char> a;
        bool  ans=true;
        for(int i=0; i<s.size(); i++)
        {
            if(s[i]==)
        }
        return ans;
    }
};
