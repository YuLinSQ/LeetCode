class Solution(object):
    def intersect(self, nums1, nums2):
        """
        :type nums1: List[int]
        :type nums2: List[int]
        :rtype: List[int]
        """
        map1 = {}
        map2 = {}
        arr = []
        for i in range (len(nums1)):
            if nums1[i] not in map1:
                map1[nums1[i]] = 1
            else:
                map1[nums1[i]] += 1
        for i in range (len(nums2)):
            if nums2[i] not in map2:
                map2[nums2[i]] = 1
            else:
                map2[nums2[i]] += 1
        for i in map1:
            if i in map2:
                temp = min(map1[i], map2[i])
                print(temp, i, map1[i], map2[i])
                for j in range (temp):
                    arr.append(i)
        return arr
