class Solution(object):
    def findDisappearedNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        s = set()
        for i in nums:
            if i not in s:
                s.add(i)
        ans = []
        for i in range(1, len(nums)+1):
            if i not in s:
                ans.append(i)
        return ans
