class Solution(object):
    def findErrorNums(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        hm = {}
        ans = [-1,-1]
        for i in nums:
            if i not in hm:
                hm[i] = 0
            hm[i]+=1
        for i in range(1, len(nums)+1):
            if i not in hm:
                ans[1] = i
            elif hm[i] == 2:
                ans[0] = i
            if ans[0] != -1 and ans[1] != -1:
                break
        return ans
        
